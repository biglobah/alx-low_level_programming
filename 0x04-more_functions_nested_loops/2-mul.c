#include "main.h"

/**
 * mul - A function to multiply two integerws
 * @a: first integer
 * @b: second integer
 * Return: multiplied integer
 */
int mul(int a, int b)
{
	int mul;

	mul = (a * b);

	return (mul);
}
