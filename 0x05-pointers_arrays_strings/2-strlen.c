#include "main.h"

/** _strlen - prints the length of the string
 * @str: string
 * Return: Length
 */
int _strlen(char *s)
{
	int i = 1;
	char pl = 0;

	for (i = 0; i <= 
	{
		sum++;
		pl =  s(i++);
	}
	return (sum);
}
